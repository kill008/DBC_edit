# 本文档记录在DBC edit项目中遇到的问题
1. getline()函数的读取DBC，遇到回车返回什么？
2. 读取文件的时候要给出绝对路径（不一定，但是只给文件名，放在同一目录也不行）
3. 读取后要用is_open 检查是否读取成功
4. vector 内不可以放引用，(因为引用需要初始化?)
    1. vector 初始化的时候是按引用还是值传递？
        貌似是值传递
5. array 内只能放标准数据类型，不可以放string